@model WebKillaDeco.Models.SubCategory
@using WebKillaDeco.Helpers
@{
    ViewData["Title"] = "Edit";
    Layout = "/Views/Shared/_LayoutEmployee.cshtml";
}

<div class="container my-5">
    <div class="card">
        <div class="card-body">
            <h1 class="card-title text-center mb-4">@Alias.EditSubCategory</h1>
            <form asp-action="Edit" method="post" enctype="multipart/form-data">
                <input type="hidden" asp-for="SubCategoryId" />

                <div class="form-group">
                    <label asp-for="Name"></label>
                    <input asp-for="Name" class="form-control" />
                    <span asp-validation-for="Name" class="text-danger"></span>
                </div>

                <div class="form-group">
                    <label asp-for="IconUrlFile"></label>
                    @if (!string.IsNullOrEmpty(Model.IconUrl))
                    {
                        <div class="mb-3">
                            <img src="@Url.Content(Model.IconUrl)" alt="Current Icon" style="max-height: 100px;" />
                        </div>
                    }
                    <input type="file" asp-for="IconUrlFile" class="form-control" />
                    <span asp-validation-for="IconUrlFile" class="text-danger"></span>
                </div>

                <div class="text-center">
                    <button type="submit" class="btn btn-primary">@Alias.Save</button>
                    <a href="@Url.Action("Index")" class="btn btn-secondary">@Alias.Cancel</a>
                </div>
            </form>
        </div>
    </div>
</div>

